[package]
name = "apex-tool-bridge"
version.workspace = true
edition.workspace = true
description = "apex tool bridge: a tool attachment driven over JSON lines on stdin and stdout, for tools in any language"

[dependencies]
apex-tool = { path = "../apex-tool" }
serde_json = "1"
