[package]
name = "apex-tool-lsp"
version.workspace = true
edition.workspace = true
description = "apex tool lsp: language servers as an apex tool, through the public API only"

[dependencies]
apex-core = { path = "../apex-core" }
apex-server = { path = "../apex-server" }
serde_json = "1"
