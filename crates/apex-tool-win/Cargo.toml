[package]
name = "apex-tool-win"
version.workspace = true
edition.workspace = true
description = "apex tool win: acme's win as an apex tool, through the public API only"

[dependencies]
apex-core = { path = "../apex-core" }
apex-server = { path = "../apex-server" }
libc = "0.2"
