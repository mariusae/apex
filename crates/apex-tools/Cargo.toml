[package]
name = "apex-tools"
version.workspace = true
edition.workspace = true
description = "apex tool: the tools that come with apex (win, lsp), through the public API only"

[dependencies]
apex-core = { path = "../apex-core" }
apex-server = { path = "../apex-server" }
serde_json = "1"
libc = "0.2"
